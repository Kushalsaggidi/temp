# Contract fixture scope

Every JSON file in this directory is synthetic contract-shape data only. These
examples are not persisted evidence, live executions, publication-gating proof,
or reproduced facts for `STATUS.md`.

Names such as `succeeded` in an execution-shaped example illustrate a union
branch. They do not claim that an application executor or integration test ran.
