# IAM team/channel TBD - decisions and assumptions

| Date and time PT | Decision or assumption | Why it was made | Evidence or source | Owner | Revisit when | Shared clarification needed? |
| --- | --- | --- | --- | --- | --- |
| 2026-09-16 01:59 PT | Use one Next.js + TypeScript modular monolith and one SQLite database; no architecture deviation. | This is the current prompt pack's locked default and Node 24 provides a working built-in SQLite driver. | Assigned brief; current prompt pack; local runtime audit | Agent 01 | Revisit only if the actual runtime proves infeasible or the team explicitly decides otherwise. | No |
| 2026-09-16 01:59 PT | Use executable Zod schemas as canonical contracts, inferred TypeScript types, and checked-in generated JSON Schema 2020-12 artifacts. | One definition can validate fixtures, persistence/API boundaries, and specialist work while remaining machine-readable. | `docs/architecture.md`; contract tests | Agent 01 | Revisit through a documented contract-change request. | No |
| 2026-09-16 01:59 PT | Standardize UTC ISO-8601 timestamps, opaque prefixed IDs, SemVer version labels, `sha256:<lowercase hex>` digests, shared actor enums, and replacement pointers to asset-version IDs. | The prompt pack requires these concepts but leaves their exact serialization open; one choice prevents specialist drift. | `src/contracts/`; `docs/architecture.md` | Agent 01 | Revisit only before dependent specialist integration or through a compatibility-reviewed contract change. | No |
| 2026-09-16 01:59 PT | Propose Property Operations Brief Builder as the bootstrap hero (`asset_property_ops_brief`, `0.1.0-draft.1`). **This is an unconfirmed team assumption.** | The brief does not prescribe a hero. This small deterministic candidate can demonstrate reuse with synthetic data without making optional AI a dependency. | `docs/product-demo.md`; assigned brief | Agent 01 | A human team member confirms or replaces the Scope Lock before Agent 02 broadens the catalog. | Yes |
| 2026-09-16 01:59 PT | Keep the bootstrap version `draft`, `reference_only`, unreviewed, and unfrozen, with permission decisions `unconfirmed`. | No human creator attestation, prototype review, executor manifest, digest, or publication evidence exists; seeding approval would fabricate evidence. | `fixtures/catalog/bootstrap-draft.json`; `docs/source-permissions.md` | Agent 01 | Real creator attestation, explicit fixture transfer, reviewed executor manifest, and governance evidence become available. | No |
| 2026-09-16 01:59 PT | Treat AssetVersion content as immutable canonical JSON and lifecycle/timestamps/replacement as a separate governance projection. Freeze with a server-generated subject digest before publication evidence. | This makes publication lifecycle-only and lets the database/tests detect evidence-invalidating content changes. | `docs/architecture.md`; current prompt pack | Agent 01 | Checkpoint B after Agent 04 supplies the reviewed manifest/digest. | No |
| 2026-09-16 01:59 PT | Keep official state in the supplied `00_Admin` files; do not create a second project-state, evidence, decision, or handoff log. | The participant package makes `STATUS.md` the canonical progress/evidence index. | Official participant templates and package README | Agent 01 | Agent 06 receives an explicit active-editor handoff for final assurance. | No |
| 2026-09-16 02:55 PT | Require JSON Schema Draft 2020-12 and a root assertion structurally in Zod types and generated schemas; identify remaining cross-field rules with `x-zod-runtime-refinements`. | A contract audit found that refinement-only dialect checks were absent from inferred types and generated artifacts. | `src/contracts/primitives.ts`; `generated/contracts/json-schema-document.schema.json`; contract tests | Agent 01 | Revisit only through a compatibility-reviewed contract request. | No |
| 2026-09-16 02:55 PT | Block publication/deprecation while any access, tool-use, or final-package permission remains `unconfirmed`; enforce the rule in the canonical contract and SQLite. | Public discovery must never expose a version before governance gates are satisfied, even if a caller bypasses a service. | `src/contracts/catalog.ts`; `db/migrations/001_initial.sql`; contract/database tests | Agent 01 | Agent 05 may propose stricter evidence requirements through the contract-change protocol. | No |
| 2026-09-16 02:55 PT | Bind persisted JSON identities to indexed columns and bind evidence to the exact frozen version digest. | Prevent repository overlays or duplicated fields from masking drift and ensure publication evidence cannot silently target different content. | `db/migrations/001_initial.sql`; `tests/database/persistence.test.ts` | Agent 01 | Revisit only with a migration-backed compatibility change. | No |

Use this file for material scope, data, tool, testing, access, reuse, and handoff decisions. Do not hide a decision only in chat.

If a clarification may affect another team working on the same use case, flag it as shared, ask the sponsor or event team to publish the answer fairly, and link the current event post or FAQ here. If sources conflict, Realpage policy controls first, followed by current event rules, the assigned brief, and current sponsor or event-team clarifications.
